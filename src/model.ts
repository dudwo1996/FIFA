export type UserInfo = {
    nickname: string;
    level: number;
    asseccId: string;
}

export type UserMatchInfo = {
    matchDate: string,
    matchId: string,
    matchInfo: [],
    matchType: number,
}